###############################################
## Setup
###############################################

set $display_help terminal -t Help -c Help -s 100x30 -l -e shortcuts
set $app_launcher rofi -show drun
set $emoji_picker rofi-emoji
set $file_manager terminal -t Files -c Files -e nnn
set_from_resource $color cursorColor
set $terminal xterm 
set $mod Mod4
floating_modifier $mod
tiling_drag modifier titlebar
bindsym $mod+q kill

# ----------> Looks
for_window [class=".*"] border pixel 1
gaps inner 20
gaps outer 5
font pango:Inter 12
client.focused $color #000000 #ffffff $color $color

# ----------> reload the configuration file
bindsym $mod+Shift+c reload
# ----------> restart i3 inplace
bindsym $mod+Shift+r restart




###############################################
## Autostart
###############################################

# ----------> network applet <------------------
# exec --no-startup-id nm-applet

# ----------> Init xterm <----------------------
exec_always --no-startup-id xrdb -merge ~/.Xresources

# ----------> Enable num lock <-----------------

# exec_always --no-startup-id numlockx on

# ----------> arandr setting <------------------
exec_always --no-startup-id sh ~/.screenlayout/default.sh

# ----------> notification daemon <-------------
exec_always --no-startup-id killall -q dunst; dunst

# ----------> autostart applications <----------
# exec --no-startup-id dex --autostart --environment i3

# ----------> lock screen <---------------------
# exec --no-startup-id xss-lock --transfer-sleep-lock -- lock-screen

# ----------> set wallpaper <-------------------
exec --no-startup-id sleep 1; ~/.fehbg 

# ----------> autotiling feature <--------------
exec_always --no-startup-id autotiling

# ----------> ensure your D-Bus environment is aware of i3 session
# exec --no-startup-id dbus-update-activation-environment --systemd DISPLAY I3SOCK XDG_CURRENT_DESKTOP

# ----------> Keyboard Layout: US and Greek | Toggle: Alt+Shift
exec --no-startup-id setxkbmap -layout us,gr -option grp:alt_shift_toggle

# ----------> Status bar <----------------------
exec --no-startup-id polybar -r &

# ----------> Load xfsettingsd <----------------
# exec --no-startup-id xfsettingsd

# ----------> Pipewire patch bay <--------------
exec --no-startup-id qpwgraph -m

# ----------> Authentication agent <------------
# exec --no-startup-id /usr/libexec/lxqt-policykit-agent &

# ----------> Clipboard managerl <--------------
# exec --no-startup-id xfce4-clipman

# ----------> Night Light <---------------------
exec --no-startup-id nightlight

# ----------> X Compositor <--------------------
# exec --no-startup-id picom --config ~/.config/picom/picom.conf

exec_always --no-startup-id pipewire &
exec_always --no-startup-id pipewire-pulse &
exec_always --no-startup-id wireplumber &


####################################################
## Use pactl to adjust volume in PulseAudio.
####################################################
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status




####################################
## Application Shortcuts
####################################

bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+w exec wallpapers
bindsym $mod+Shift+Return exec $file_manager
bindsym $mod+d exec --no-startup-id $app_launcher
bindsym $mod+e exec --no-startup-id $emoji_picker
bindsym $mod+h exec $display_help
bindsym $mod+c exec terminal -c BsdConfWindow -l -s 100x30 -e 'sudo bsdconfig'
# bindsym Print exec --no-startup-id rofi-grab


####################################
## Windows & Desktop Shortcuts
####################################

# ----------> change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# ----------> alternatively, you can use the cursor keys
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# ----------> move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# ----------> alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# ----------> split in horizontal orientation
# bindsym $mod+h split h

# ----------> split in vertical orientation
# bindsym $mod+v split v

# ----------> enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# ----------> change container layout (stacked, tabbed, toggle split)
# bindsym $mod+s layout stacking
# bindsym $mod+w layout tabbed
# bindsym $mod+e layout toggle split

# ----------> toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# ----------> change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# ----------> focus the parent container
bindsym $mod+a focus parent

# ----------> focus the child container
#bindsym $mod+d focus child

# ----------> Define workspaces names
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# ----------> switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# ----------> move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10




####################################
## Modes
####################################

# ----------> Power Mode
set $mode_system System (L)ock (S)hutdown (R)estart (E)xit
mode "$mode_system" {
    bindsym l exec i3power lock, mode "default"
    bindsym s exec i3power shutdown, mode "default"
    bindsym r exec i3power reboot, mode "default"
    bindsym e exec i3power logout, mode "default"
    
	# Exit keys
    bindsym Return 	mode "default"
    bindsym Escape 	mode "default"
    bindsym q 		mode "default"
    bindsym $mod+x 	mode "default"
}
bindsym $mod+x mode "$mode_system

# ----------> Screenshot Mode (PrtScr)
set $mode_screenshot Screenshot: (f)ull (w)indow (s)election
mode "$mode_screenshot" {
    bindsym f exec --no-startup-id prtscr shot-full, mode "default"
    bindsym w exec --no-startup-id prtscr shot-window, mode "default"
    bindsym s exec --no-startup-id prtscr shot-sel, mode "default"

    # Exit keys
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym q      mode "default"
    bindsym Print  mode "default"
}
bindsym Print mode "$mode_screenshot"

# ----------> Recording Mode (Ctrl+PrtScr)
set $mode_record Record: (f)ull (w)indow (s)election s(t)op
mode "$mode_record" {
    bindsym f exec --no-startup-id prtscr rec-full, mode "default"
    bindsym w exec --no-startup-id prtscr rec-window, mode "default"
    bindsym s exec --no-startup-id prtscr rec-sel, mode "default"
    bindsym t exec --no-startup-id prtscr stop, mode "default"

    # Exit keys
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym q      mode "default"
    bindsym Control+Print mode "default"
}
bindsym Control+Print mode "$mode_record"


# ----------> resize window
mode "resize" {
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}
bindsym $mod+r mode "resize"



####################################
## Window Rules
####################################

for_window [class="Wallpapers"] floating enable, border pixel 2, resize set 640 400, move position 40 50
for_window [class="Arandr"] floating enable, border pixel 2, resize set 800 400, move position center
for_window [class="Lxappearance"] floating enable, border pixel 2, resize set 700 600, move position center
for_window [class="Gnome-abrt"] floating enable, border pixel 2, resize set 900 700, move position center
for_window [class="Lightdm-gtk-greeter-settings"] floating enable, border pixel 2, resize set 900 700, move position center
for_window [class="qpwgraph"] floating enable, border pixel 2, resize set 1900 1000, move position center
for_window [class="NetConfWindow"] floating enable, move position center
for_window [class="Eww"] floating enable
for_window [class="pavucontrol"] floating enable, border pixel 2, resize set 600 600, move position center
for_window [class="Xfce4-clipman-history"] floating enable, border pixel 2, resize set 600 700, move position center
for_window [class="Help"] floating enable, move position center
for_window [class="PulseMixer"] floating enable, move position center
for_window [title="TopWindow"] floating enable,move position center
for_window [class="CalcurseWindow"] floating enable, move position center
for_window [class="weatherWindow"] floating enable, move position center
for_window [class="NcduWindow"] floating enable, move position center
for_window [class="BsdConfWindow"] floating enable, move position center
